PORT=3000
SECRET_KEY=change-me

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=
DB_NAME=forever

# Optional: when set, server connects and uses Redis (login rate limit + /health ping).
REDIS_ADDR=
REDIS_PASSWORD=
REDIS_DB=
